# 로컬 개발 프로파일 (SPRING_PROFILES_ACTIVE=local — bootRun 기본값).
# server/.env(KEY=VALUE)를 property source 로 로드한다. 시크릿(JWT_SECRET·DB 비번 등)은 .env 로만 주입(커밋 금지).
# DB/Redis localhost 기본값은 application.yml 의 ${...:기본} 으로 이미 적용된다.
spring:
  config:
    import: "optional:file:.env[.properties]"
