spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/ieoseo
    username: ieoseo
    password: ieoseo
  data:
    redis:
      host: localhost
      port: 6379
      password: ""
  # Supabase Auth(Resource Server) — 로컬 placeholder 공개값. 실제 프로젝트로 교체(ADR-0014).
  security:
    oauth2:
      resourceserver:
        jwt:
          jwk-set-uri: https://YOUR-PROJECT.supabase.co/auth/v1/.well-known/jwks.json

ieoseo:
  sentry:
    dsn: ""
    environment: local
    release: ""
    traces-sample-rate: 0.0
