# 테스트 프로파일 (SPRING_PROFILES_ACTIVE=test — build.gradle.kts 에서 지정).
# 실 DB/Redis 없이 컨텍스트가 로드되도록 DataSource/JPA/Redis auto-config 를 제외한다.
# 공통(application.yml)이 안 주는 환경값(jwt secret·oauth·sentry)만 테스트 고정값으로 둔다.
spring:
  flyway:
    enabled: false
  autoconfigure:
    exclude:
      - org.springframework.boot.jdbc.autoconfigure.DataSourceAutoConfiguration
      - org.springframework.boot.hibernate.autoconfigure.HibernateJpaAutoConfiguration
      - org.springframework.boot.data.jpa.autoconfigure.DataJpaRepositoriesAutoConfiguration
      - org.springframework.boot.data.redis.autoconfigure.DataRedisAutoConfiguration
      - org.springframework.boot.data.redis.autoconfigure.DataRedisRepositoriesAutoConfiguration

ieoseo:
  # 테스트 전용 더미 JWT 시크릿(실 시크릿 아님). access/refresh TTL 은 공통에서 상속.
  jwt:
    secret: test-only-jwt-secret-please-change-32bytes-min
  oauth:
    google:
      client-id: ""
    apple:
      client-id: ""
    kakao:
      client-id: ""
  sentry:
    dsn: ""
    environment: test
    release: ""
    traces-sample-rate: 0.0
